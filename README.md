# Codex AI Course Directory

This repository contains a minimal web page that lists a few popular AI-related courses. The page provides simple filtering and sorting controls so you can quickly find what you want.

## Usage
Open `index.html` in a web browser. You can then filter courses by provider or sort them by title or provider.

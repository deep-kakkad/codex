[build]
  publish = "public"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
  # public/validate.js is imported by lib/engine.js as the single source of
  # validation rules, shared with the browser.
  included_files = ["lib/**", "public/validate.js"]
